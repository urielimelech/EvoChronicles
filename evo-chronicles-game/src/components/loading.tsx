import React from "react";
import { StyledLoading } from "../styles/styled-loading";

export const Loading = () => {
    return (
        <StyledLoading>
            <p>Loading...</p>
        </StyledLoading>
    )
}